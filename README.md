npx create-next-app@latest nombre_del_proyecto
ir a la carpeta del proyecto
hacer npm install
copiar el next.config.js para aceptar imagenes externas
instalar json-server
npm install json-server concurrently @heroicons/react
configurar package.json el scripts
hacer la estructura
cuando tenga el archivo json, lo abro y copio las cosas en el db.json, todo dentro de "products"
instalar zod
npm install react-hook-form zod @hookform/resolvers
para insertar imágenes en el URL ir a la imagen click derecho - copiar dirección de imagen y ese es el link que se pone
instalar framer-motion
npm install framer-motion
npm run dev
git init
git add .
git commit -m "Primer commint"
crear el repositorio en gitHub
copiar lo que dice de :
git remote add origin https://...
git branch -M main
git push -u origin main
969998